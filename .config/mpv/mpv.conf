hwdec=auto-safe
vo=gpu
gpu-api=vulkan

# Per-machine overrides. Seeded from local.template.conf by krypt; anything set
# there wins, because mpv applies options in file order.
include=~/.config/mpv/local.conf
